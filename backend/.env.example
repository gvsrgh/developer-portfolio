# Server Configuration
PORT=8000

# Email Configuration (REQUIRED for contact form to work)
# Replace with your actual Gmail credentials
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password
RECIPIENT_EMAIL=your-email@gmail.com

# Gmail App Password Setup Instructions:
# 1. Go to Google Account settings (myaccount.google.com)
# 2. Security > 2-Step Verification (MUST be enabled first)
# 3. Security > App passwords > Generate new app password
# 4. Select "Mail" and your device
# 5. Use the generated 16-character password (format: xxxx xxxx xxxx xxxx) as EMAIL_PASS
# 6. DO NOT use your regular Gmail password
# 7. The app password should be 16 characters without spaces in the .env file

# Example format:
# EMAIL_PASS=abcdeghjklmnopqr

# Alternative Email Providers:
# For other email providers, update the nodemailer configuration in index.ts
# Examples: Outlook, Yahoo, custom SMTP servers

# Note: If email credentials are not set, the contact form will show an error
# but the backend will still log contact submissions to the console